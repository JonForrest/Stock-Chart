<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/main.css" />
    <title>Test Stock Chart</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
  </head>
  <body class="adjust">
    
    <%# Division One %>
    <div id="divisionOne" class="division">
      <%# Div Per Stock Symbol %>
      <div class="chartContainer">
        <%# Div for line graph %>
        <canvas id="myChartOne" ></canvas>
      </div>
      <div class="optionPanel"><%# Div for options %>
    </div>

      <%# Write for each loop for various data sets %>
    </div>

    <%# Division Two %>
    <div id="divisionTwo" class="division">
      <%# Div Per Stock Symbol %>
      <div class="chartContainer">
        <%# Div for line graph %>
        <canvas id="myChartTwo" ></canvas>
      </div>
      <div class="optionPanel"><%# Div for options %>
    </div>

      <%# Write for each loop for various data sets %>
    </div>

    <%# Division Three %>
    <div id="divisionThree" class="division">
      <%# Div Per Stock Symbol %>
      <div class="chartContainer">
        <%# Div for line graph %>
        <canvas id="myChartThree" ></canvas>
      </div>
      <div class="optionPanel"><%# Div for options %>
    </div>

      <%# Write for each loop for various data sets %>
    </div>
    
    <%# put your script tags below this config file for github %>
    
    <script type="text/javascript" src="config.js"></script>
  
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>//SCRIPTFIX itterate three times to populate each graph
        const ctx = document.getElementById('myChartOne').getContext("2d");//SCRIPTFIX change element selected each itteration
      
        const labels=[ //SCRIPTFIX pass in new info for all data and labels
            '1/2023',
            '2/2023',
            '3/2023',
        ];

        const data={
          labels,
          datasets:[{
              //data is the points on the graph
              data: [35, 22, 17],
              //this label is the label at the top of the graph
              label: "Sales",
          }],
        };

        const config={
          type:'line',
          data: data,
          options:{
            responsive:true,
          },
        };

        const myChart=new Chart(ctx, config)
      </script>


  </body>
</html>
